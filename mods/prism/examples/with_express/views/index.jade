!!! 5
html(lang="en")
  head
    meta(charset="utf-8")
    each tag in assetTags
      if tag.type == 'js'
        script(type="text/javascript", src="/#{tag.filename}")
      else
        link(type="text/css", rel="stylesheet", href="#/{tag.filename}")
    title Prism Demo
  body
    h1 Welcome!
    p Open the console and type `api` to inspect the client-side APIs returned from the server.